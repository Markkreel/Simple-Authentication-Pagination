// This file configures Next.js-specific settings and features.
// It allows customization of routing, build options, and other Next.js behaviors.

import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;
